import { ElementMap } from '@graftini/graft';

type RenderProps = {
  componentId?: string;
  markup: ElementMap;
};

export default function Render({ componentId, markup }: RenderProps) {
  // TODO: How to render the new markup.
  return <></>;
}
